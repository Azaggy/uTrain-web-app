

spring.datasource.url=jdbc:mysql://localhost:3306/utrain-web-app
spring.datasource.username=utrain1
spring.datasource.password=12345678!

api.openweathermap.key=6576cdcaabd79cc15b4a75a11ded0fe5
weather.url=api.openweathermap.org/data/2.5/weather

# Specify the DBMS
spring.jpa.database = MYSQL

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

